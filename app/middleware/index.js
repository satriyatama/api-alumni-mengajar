const validation = require('./validation')
const verification = require('./verification')
const uniqueData = require('./uniqueData')
const dataManipulation = require('./dataManipulation')

module.exports = {
  validation,
  verification,
  uniqueData,
  dataManipulation,
}